#include <stdio.h>
#include <math.h>


int main()
{
    double a, b, c;

    printf("Enter side A ");
    scanf("%lf", &a);
    printf("\nEnter side B ");
    scanf("%lf", &b);

    c = sqrt(a * a + b * b);

    printf("Result is: %.2f\n", c);
    
    return 0;
}
